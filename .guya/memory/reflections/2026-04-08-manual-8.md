# Reflection — 2026-04-08 (Manual 8) — Pre-commit Hook Hardening

## Context

Session focus: debug + harden the pre-commit review hook. Daniel asked to see the hook, then asked where to configure trigger conditions vs behavior conditions, then asked to commit. Pre-commit gate fired (correctly), blocked. Ran karpathy-review (9 issues), made fixes, ran review-followup (1 real bug), fixed, committed as 6e49e29.

## What Daniel should take away

- **Convergence worked.** One task, end-to-end, no drift. The two-pass review caught a real bug. The harness he built worked on the harness's own code.
- **Cache instinct improved.** He correctly diagnosed that the first commit attempt was firing the cached hook, not the source — recognized multi-state system without prompting.
- **Pattern to keep watching:** When my answer doesn't match his mental model, push harder the first time. He had to ask twice about the conditions file because I gave a wrong-but-confident answer.

## What Guya should change

- **Disambiguate before answering.** "Where are the conditions?" had two valid readings (trigger vs behavior). I picked one and was wrong. Next time, ask which one or show both.
- **Read code more carefully on first review pass.** Missed `find()` vs `findLast()` — exactly the bug class an initial review should catch. Was too anchored on stdin/observability concerns.
- **Batch independent tool calls.** Made too many sequential edits when some were independent. Hooks were noisy about it but technically right.

## Concrete fixes shipped

- Extracted `readStdin` to `hook-utils.mjs` (eliminated duplication across decision-gate + pre-commit-review)
- Added observability logging to all silent catches
- Fixed `find()` → `findLast()` for evidence ordering bug
- Cleared stale `contentHash` before recompute
- Fixed TOCTOU regex to match end-of-string
- Replaced fragile `--stat` parsing with `--numstat`
- Fixed glob `replace` to use regex for multiple wildcards
- Removed `scripts/` from pathExempt
