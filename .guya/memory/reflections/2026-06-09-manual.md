# 2026-06-09 — Reflection (manual)

## Session Summary

Two workstreams, all guya-repo. (1) **Pre-commit gate fixes**: the global review gate kept false-blocking Constantia reflection-log commits; root-caused to the arg-parser counting an unexpanded `$LOG` shell token as a non-exempt file, fixed via `isShellExpansion` (drops `$ \` * ? [ ] { }` tokens) + a per-repo `"disabled": true` opt-out that Constantia uses. 31/31 tests, full review-gate pass (hit evidence-tree drift, re-ran deep-review). (2) **Two engineering skills from Matt Pocock's set**: reviewed all 10, kept 2, skipped 8. Rebuilt the never-used `guya-decision-bugfix` planning harness into a diagnosis-&-fix loop (absorb-not-swap: grafted blast-radius + patch-vs-root-cause); built new `guya-architecture` (deepening opportunities, HTML before/after report, find-and-design → hands execution to refactor). Both push+scribed.

## What Daniel should take away

You converged hard — the opposite of your usual over-exploring. Picked 2 of 10 skills before I finished the analysis, killed the eval workspace the moment it wasn't useful, "commit and move on." Decisive and *sequential*: each thread closed before the next opened. That's the mode you want to reproduce.

Three calls in a row optimized the tool to how you actually work, not to what's textbook-correct: "never used bugfix, replace it" / "call it guya-decision-bugfix, I type these" / "delete the eval, I invoke manually." Each overrode a best-practice default with a sharper read of your real workflow. For a personal system the right question isn't "best practice" — it's "what fits how I operate," and you kept asking it.

You fixed the system instead of memorizing the workaround. Yesterday's reflection says "never chain git add && commit — the gate false-blocks." Today you said "fix the arg-parser bug too," and that class of false-block is now gone. Turning "remember not to do X" into "X is now safe" is the highest-leverage move, and you reached for it instead of settling for the local unblock.

## What Guya should change

**Verbosity, again — and you had to explicitly stop me.** Asked "what is the underlying hook bug?", I gave multiple paragraphs, you came back with "explain in one line." Exact plain-language debt flagged in the 2026-06-08 reflection, recurring within 24h. Naming it isn't working. New default: lead with the one-liner, expand only if asked.

**Over-built the trigger eval for a workflow Daniel doesn't have.** Ran a 20-query / 3-router-judge trigger-accuracy harness on guya-decision-bugfix, then he said he invokes manually and to delete it. The signal was there (every decision skill is `/`-invoked; he never mentioned auto-trigger) — I should have asked "do you rely on auto-triggering?" before spending the effort, not discovered it at cleanup. Fit the tool to how he works up front. Saved as `feedback_manual_skill_invocation`.

## Key decisions

- **Per-repo `disabled` flag over killing the gate globally.** Daniel asked to disable the whole gate; pushed back — global kill un-gates SDF/voice-chat/lina (the hook's own history records SDF "ran ungated for weeks" as a bug). Scoped opt-out for Constantia (prose/data) instead; code repos stay gated.
- **Absorb-not-swap when replacing guya-decision-bugfix.** Surfaced the 2026-05-04 latent-value tension ("never used" was the wrong cull criterion then). Passes the test here because it's replace-as-upgrade: grafted bugfix's blast-radius + patch-vs-root-cause bones into the diagnosis loop, so latent value is upgraded, not lost.
- **Read CLAUDE.md ADR table, not a new `CONTEXT.md`/`docs/adr/`.** Adapted both skills to existing Guya doc surfaces rather than importing Matt Pocock's competing doc system — single-source-of-truth belief.
- **`guya-architecture` is find-and-design only.** Hands the actual restructure to `/guya-decision-refactor`, mirroring the new bugfix→refactor pairing. Two find/diagnose skills, one execution skill.

## Growth observations

- (P2) Decisive convergence — picked 2/10 skills fast, killed an artifact on sight, sequenced threads. The inverse of his over-exploring default; worth reinforcing.
- (P2) Optimized tooling to his real workflow over abstract correctness three times in a row (replace-unused, name-for-typing, delete-unneeded-eval). Mature systems judgment grounded in self-knowledge.
- (P2) Directed a structural fix ("fix the arg-parser too") rather than accepting the local unblock — attacks the bug class, not the instance. Same instinct as yesterday's `isUnit123` single-source-of-truth call.
- Engaged the report-format / name forks on the merits (chose HTML report, overrode my markdown rec; chose the consistent name, overrode my guya-bugfix rec) — doesn't rubber-stamp recommendations.

## Open threads

- **Restart Claude Code** to pick up `/guya-decision-bugfix` (now the diagnosis loop) + `/guya-architecture` — committed + cache-synced, but this session runs the old skill table.
- Carry-forward pile still unswept (from 2026-06-08): nanoclaw#3 visual confirm, nanoclaw#2 @Andy re-skin, `telos/.guya` litter, P-023/P-024 Telos triage, close guya#5. None will ever be the "main thing" — needs a deliberate sweep.
- The 8 skipped Matt Pocock skills remain available if a need surfaces (esp. the issue-workflow cluster if Daniel ever runs a real issue queue).

## Artifacts produced

- guya repo: `2f16faa` (gate arg-parser fix + disable flag, 31/31 tests), `d5afc8a` (scribe), `ef2dde8` (guya-decision-bugfix rebuild), `108a410` (guya-architecture), `580e4a7` (scribe). All pushed.
- constantia: `8a87acd`→`bfcfee9` (`.guya/pre-commit-config.json` disable flag; rebased through the documented MANIFEST-regen conflict). Pushed.
- Memory: new `project_guya_plugin_source_vs_cache`, `feedback_manual_skill_invocation`.
- Cloned `mattpocock/skills` to /tmp for the review (throwaway).
