# Reflection — 2026-04-08 (Session 5)

## What Daniel should take away
- Caught Guya claiming "pre-commit hook working correctly" without real verification. Both commits bypassed the review gate entirely — first via pathExempt (hooks/ directory), second via reviewExempt (all .md/.json files). The "ask why" habit is becoming natural.
- Learned Claude Code hook system from first principles in one session: stdin/stdout JSON contract, Pre/Post timing, two-layer filtering (matcher → in-hook regex), gate pattern, git hooks vs Claude hooks trust model.
- One misconception corrected: matchers only see tool name, not command args. All hooks with same matcher fire on every call, each self-filters.
- Made two good config decisions: removed hooks/ from pathExempt (hook code shouldn't exempt itself), tightened small change to 10 lines with no maxFiles (line count sufficient).

## What Guya should change
- Claimed verification without actually testing the gate path. Small change bypass meant 1-line test change trivially passed. Should have recognized this and designed a test that would actually trigger the gate (>10 lines of non-exempt code).
- Stash detour was unnecessary complexity. Direct hook invocation via `bash .git/hooks/pre-commit` was the right test for the git layer. Should have done that from the start.
- Teaching mode worked well — short concepts, immediate quiz, corrections. The organic-to-formal learn transition was natural. Keep this pattern.
